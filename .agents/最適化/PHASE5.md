# フェイズ5: JavaScript実装レベルの追加最適化

## 方針

フェイズ5では、探索深度、合法手の生成順、アルファベータ枝刈り条件、評価ロジックを変えない。
対象はJavaScript実装レベルの細かい実行コストだけに限定する。

## 変更内容

### 9方向の座標差分を事前計算する

`Rule.getCanMovePanelX()` と `Rule.hasCanMovePanelX()` では、これまで9方向ループの中で毎回次の計算をしていた。

- `~~(i % 3) - 1`
- `~~(i / 3) - 1`

これを `Rule.MOVE_X` と `Rule.MOVE_Y` の `Int8Array` に事前計算した。

```ts
static readonly MOVE_X = new Int8Array([-1, 0, 1, -1, 0, 1, -1, 0, 1])
static readonly MOVE_Y = new Int8Array([-1, -1, -1, 0, 0, 0, 1, 1, 1])
```

これにより、合法手生成のホットパスで剰余、除算、整数化を繰り返さず、固定テーブル参照だけで座標差分を得る。

### `getNodeMap()` の中間配列を減らす

`Rule.getNodeMap()` は、各駒ごとに `Rule.getCanMovePanelX()` を呼び出して `number[]` の合法手配列を作ってから、子局面を作っていた。

変更後は、`getNodeMap()` 内で同じ順序の9方向ループを直接回し、合法手が見つかった時点で子局面を生成する。
これにより、探索中に大量に作られる一時的な `canMove` 配列を避ける。

維持していること:

- 盤面走査順は `Rule.NUMBERS[0..35]` のまま。
- 方向走査順は `moveIndex = 0..8` のまま。
- 移動可否条件は `getCanMovePanelX()` と同じ。
- 子局面の生成順は変更しない。

### ループ内の静的参照をローカル化する

9方向ループでは、次の静的参照をローカル変数に持つようにした。

- `Rule.MOVE_X`
- `Rule.MOVE_Y`
- `Rule.PIECES[number + 8]`

クラス静的プロパティの参照をホットループ内で繰り返さず、単純なローカル変数参照に寄せる。

## 計測結果

コマンドは `npm test`。
単位はms。

| run | depth 1 | depth 2 | depth 3 | depth 4 | depth 5 | depth 6 | depth 7 | depth 8 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 0.040 | 0.163 | 0.564 | 3.836 | 29.929 | 87.801 | 611.022 | 3639.453 |
| 2 | 0.036 | 0.151 | 0.557 | 3.676 | 30.982 | 93.738 | 647.738 | 3782.030 |
| 3 | 0.035 | 0.168 | 0.620 | 3.719 | 30.030 | 88.988 | 609.627 | 3658.146 |
| 4 | 0.038 | 0.167 | 0.540 | 3.704 | 30.023 | 88.697 | 613.620 | 3661.714 |
| 5 | 0.037 | 0.153 | 0.534 | 3.672 | 29.698 | 89.289 | 608.713 | 3652.464 |

## 平均比較

単位はms。

| depth | フェイズ4平均 | フェイズ5平均 | 差分 |
| --- | ---: | ---: | ---: |
| 1 | 0.063 | 0.037 | -0.026 |
| 2 | 0.179 | 0.160 | -0.019 |
| 3 | 0.535 | 0.563 | +0.028 |
| 4 | 3.822 | 3.721 | -0.101 |
| 5 | 30.778 | 30.132 | -0.646 |
| 6 | 91.450 | 89.703 | -1.747 |
| 7 | 636.483 | 618.144 | -18.339 |
| 8 | 3733.492 | 3678.761 | -54.731 |

## 確認

- `npm test` は5回すべて成功。
- `npx tsc --noEmit` は成功。
- ロジック変更を避けるため、探索深度、合法手の走査順、終局判定、評価値計算には手を入れていない。
